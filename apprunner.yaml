version: 1.0
runtime: python311

build:
  commands:
    build:
      - python -V

run:
  runtime-version: "3.11.13"
  command: python -c "import time; print('OK'); time.sleep(3600)"
  network:
    port: 8000

